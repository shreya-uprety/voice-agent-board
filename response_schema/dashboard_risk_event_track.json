{
  "type": "OBJECT",
  "properties": {
    "risks": {
      "type": "ARRAY",
      "items": {
        "type": "OBJECT",
        "properties": {
          "t": { "type": "STRING", "description": "ISO 8601 Timestamp" },
          "riskScore": { "type": "NUMBER", "description": "0 to 10 scale" },
          "factors": {
            "type": "ARRAY",
            "items": { "type": "STRING" },
            "description": "List of contributing factors to this score"
          }
        },
        "required": ["t", "riskScore", "factors"]
      }
    },
    "events": {
      "type": "ARRAY",
      "items": {
        "type": "OBJECT",
        "properties": {
          "event": { "type": "STRING", "description": "Title of the clinical event" },
          "t": { "type": "STRING", "description": "ISO 8601 Timestamp" },
          "note": { "type": "STRING", "description": "Brief description of the event" }
        },
        "required": ["event", "t", "note"]
      }
    }
  },
  "required": ["risks", "events"]
}